import type {Category} from "../types";
import {zh} from "../i18n";
import {size} from "../format";

export interface ScanProgressState{plugin:string;category:Category|null;path:string;pluginIndex:number;totalPlugins:number;completedPlugins:number;visited:number;found:number;bytes:number}
export const emptyProgress:ScanProgressState={plugin:"",category:null,path:"",pluginIndex:0,totalPlugins:0,completedPlugins:0,visited:0,found:0,bytes:0};
export function ScanningStep({progress,onCancel}:{progress:ScanProgressState;onCancel:()=>void}){const percent=progress.totalPlugins?Math.round(progress.completedPlugins/progress.totalPlugins*100):0;return <section className="screen scanning-screen"><div className="scan-orbit"><div className="scan-pulse">⌁</div></div><h2>{zh.scanningTitle}</h2><p>{zh.scanningHint}</p><div className="progress-panel"><div className="progress-label"><span>{progress.category?zh.category[progress.category]:zh.waiting}</span><b>{progress.completedPlugins}/{progress.totalPlugins||"—"}</b></div><div className="progress-track"><span style={{width:`${percent}%`}}/></div><div className="scan-stats"><div><span>{zh.currentPlugin}</span><b>{progress.plugin||"—"}</b></div><div><span>{zh.visited}</span><b>{progress.visited}</b></div><div><span>{zh.found}</span><b>{progress.found}</b></div><div><span>{zh.accumulated}</span><b>{size(progress.bytes)}</b></div></div><div className="current-path"><span>{zh.currentPath}</span><code>{progress.path||zh.waiting}</code></div></div><button className="danger" onClick={onCancel}>{zh.cancelScan}</button></section>}
